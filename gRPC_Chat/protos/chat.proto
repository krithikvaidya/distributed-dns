
// chat.proto 
// Contains Protocol Buffers definitions and gRPC service definition.

syntax = "proto3";

package grpc_chat;

import "google/protobuf/timestamp.proto";

option go_package="github.com/IEEE-NITK/distributed-dns/gRPC_Chat/protos";

service Chat {

    // RPCs for Login, Logout, Streaming

}

// Create protobuf messages for Login (Request and Response), Logout (Request and Response), 
// MessageStream (Request and Response).


// Compile the .proto file with protoc
